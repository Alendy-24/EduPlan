{
  "name": "eduplan-dev",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others --names frontend,data-integration \"npm run dev --prefix frontend\" \"npm run dev --prefix data-integration\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
