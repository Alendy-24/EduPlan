import { useEffect, useState } from 'react';
import { Link, useLocation, useParams } from 'react-router-dom';
import BookmarkButton from '../components/BookmarkButton';
import DemoNotice from '../components/DemoNotice';
import { demoInstitution, libraryImage, studyImage } from '../data/mock/catalog';
import { getInstitutionByCode } from '../services/institutions';
import './pages.css';

const tabs=['Información','Programas','Admisión','Becas','Vida estudiantil'];
function safeWebsite(value){if(!value?.trim())return null;try{const url=new URL(/^https?:\/\//i.test(value)?value:`https://${value}`);return ['http:','https:'].includes(url.protocol)&&url.hostname.includes('.')&&!url.username&&!url.password?url.href:null}catch{return null}}
export default function InstitutionDetailPage(){
  const {institutionId}=useParams();const {state}=useLocation();const demo=institutionId===demoInstitution.id;
  const [institution,setInstitution]=useState(demo?demoInstitution:state?.institution||null);
  const [loading,setLoading]=useState(!demo&&!state?.institution);
  const [error,setError]=useState('');const [tab,setTab]=useState('Información');
  useEffect(()=>{if(demo)return;const controller=new AbortController();getInstitutionByCode(institutionId,controller.signal).then(value=>{setInstitution(current=>current?.code===value.code?{...current,...value}:value);setError('')}).catch(reason=>{if(reason.name!=='AbortError')setError('No pudimos cargar los datos actualizados de esta institución.')}).finally(()=>{if(!controller.signal.aborted)setLoading(false)});return()=>controller.abort()},[institutionId,demo]);
  if(loading&&!institution)return <main className="page container"><p role="status">Cargando institución...</p></main>;
  if(!institution)return <main className="page container"><Link className="back-link" to="/instituciones">← Volver a resultados</Link><div className="empty-state surface"><h1>Detalle no disponible</h1><p role="alert">{error||'No encontramos esta institución.'}</p><Link className="btn btn-primary" to="/instituciones">Ver instituciones</Link></div></main>;
  const name=institution.name,city=demo?institution.city:institution.municipality,sector=institution.sector,character=demo?institution.character:institution.academicCharacter,website=safeWebsite(institution.website);
  function share(){navigator.clipboard?.writeText(window.location.href)}
  return <main><div className={`detail-banner${demo?"":" is-empty"}`}>{demo?<img src={demoInstitution.image} alt="Campus universitario de referencia"/>:<div className="detail-banner-placeholder">Imagen institucional no disponible en el catálogo</div>}</div><div className="container"><div className="detail-header"><div><Link className="back-link" to="/instituciones">← Volver a resultados</Link><h1>{name}</h1><div className="metadata"><span>{sector||'Sector no disponible'}</span><span>{character||'Tipo no disponible'}</span><span>{city||'Ciudad no disponible'}</span></div></div><div className="detail-actions"><BookmarkButton id={`institution-${institutionId}`} label={name}/><button className="btn btn-secondary" type="button" onClick={share}>Compartir</button>{website&&<a className="btn btn-primary" href={website} target="_blank" rel="noreferrer">Sitio web ↗</a>}</div></div>{error&&<p className="notice" role="alert" style={{marginTop:18}}>{error} Mostramos los datos disponibles de los resultados.</p>}{demo&&<div style={{marginTop:18}}><DemoNotice/></div>}<div className="tabs detail-tabs" role="tablist" aria-label="Información de institución">{tabs.map(item=><button key={item} type="button" className="tab" role="tab" aria-selected={tab===item} onClick={()=>setTab(item)}>{item}</button>)}</div>{tab==='Información'?<><div className="detail-grid"><section className="detail-copy"><h2>Sobre la institución</h2><p>{demo?institution.summary:'Los datos de esta institución provienen del catálogo público consultado por EduPlan. Para programas, admisiones y vida estudiantil, visita su sitio oficial.'}</p>{institution.address&&<p><strong>Dirección:</strong> {institution.address}</p>}{institution.phone&&<p><strong>Teléfono:</strong> {institution.phone}</p>}<p>Revisa su ubicación y carácter académico antes de comparar opciones.</p></section><aside className="facts surface"><h2>Datos disponibles</h2><div className="fact-row"><span>Ciudad</span><strong>{city||'No disponible'}</strong></div><div className="fact-row"><span>Departamento</span><strong>{institution.department||'No disponible'}</strong></div><div className="fact-row"><span>Sector</span><strong>{sector||'No disponible'}</strong></div><div className="fact-row"><span>Carácter</span><strong>{character||'No disponible'}</strong></div><div className="fact-row"><span>Modalidades</span><strong>{institution.modalities?.join(', ')||'No disponible'}</strong></div></aside></div>{demo&&<section className="landing-section"><h2>Espacios para estudiar</h2><p className="subtle">Imágenes de referencia para la vista de ejemplo.</p><div className="gallery"><img src={demoInstitution.image} alt="Edificio universitario de referencia"/><img src={libraryImage} alt="Biblioteca de referencia"/><img src={studyImage} alt="Estudiantes de referencia"/></div></section>}</>:<section className="detail-grid"><div className="detail-copy"><h2>{tab}</h2><p>{tab==='Programas'?'Esta sección mostrará la oferta académica cuando se conecte el servicio de programas.':`La información de ${tab.toLocaleLowerCase('es')} aún no está disponible en este catálogo. Consulta la fuente oficial de la institución.`}</p>{tab==='Programas'&&demo&&<Link className="btn btn-primary" to={`/instituciones/${institutionId}/programas`}>Explorar vista de programas</Link>}</div><aside className="cta-panel"><h3>Información oficial</h3><p>Confirma los detalles directamente con la institución antes de tomar una decisión.</p>{website&&<a className="btn btn-secondary" href={website} target="_blank" rel="noreferrer">Visitar sitio web ↗</a>}</aside></section>}</div></main>
}
