import { useState } from 'react';
import { Link } from 'react-router-dom';
export default function AuthPage({mode='login'}){const register=mode==='register';const [showPassword,setShowPassword]=useState(false);const [message,setMessage]=useState('');function submit(e){e.preventDefault();setMessage('Este formulario es una vista previa. El acceso estará disponible cuando se conecte la autenticación.')}return <main className="auth-main"><div className="auth-card"><h1>{register?'Crea tu cuenta':'Bienvenido a EduPlan'}</h1><p className="subtle">Tu futuro, con más información.</p><form className="auth-form" onSubmit={submit}>{register&&<label className="field">Nombre completo<input type="text" autoComplete="name" required placeholder="Tu nombre"/></label>}<label className="field">Correo electrónico<input type="email" autoComplete="email" required placeholder="tu@correo.edu.co"/></label><label className="field">Contraseña<input type={showPassword?'text':'password'} autoComplete={register?'new-password':'current-password'} required minLength={8} placeholder="Tu contraseña"/></label><button className="text-link" style={{justifySelf:'start',border:0,background:'none',padding:0}} type="button" onClick={()=>setShowPassword(!showPassword)}>{showPassword?'Ocultar contraseña':'Mostrar contraseña'}</button><button className="btn btn-primary" type="submit">{register?'Crear cuenta':'Iniciar sesión'}</button>{message&&<p className="auth-feedback" role="status">{message}</p>}</form><div className="auth-divider">o continúa con</div><div className="social-buttons"><button type="button" className="btn btn-secondary" disabled title="Disponible al conectar autenticación">Continuar con Google</button><button type="button" className="btn btn-secondary" disabled title="Disponible al conectar autenticación">Continuar con Microsoft</button></div>{!register&&<p className="auth-foot"><button className="text-link" style={{border:0,background:'none'}} type="button" onClick={()=>setMessage('La recuperación de contraseña estará disponible al conectar la autenticación.')}>¿Olvidaste tu contraseña?</button></p>}<p className="auth-foot">{register?'¿Ya tienes una cuenta?':'¿Aún no tienes cuenta?'} <Link className="text-link" to={register?'/login':'/register'}>{register?'Inicia sesión':'Crear cuenta'}</Link></p></div></main>}
